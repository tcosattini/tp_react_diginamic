import React from 'react';

const Navigation = () => {
  return (
    <header>
      <nav className="flex items-center justify-evenly">
        <a href="/">Home</a>
      </nav>
    </header>
  );
};

export default Navigation;
